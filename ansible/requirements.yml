# Requirements for Ansible collections and roles used by this project.
# Install with:
#   ansible-galaxy collection install -r ansible/requirements.yml

collections:
  - name: community.crypto
  - name: community.mysql
  - name: community.proxmox
  - name: ansible.posix


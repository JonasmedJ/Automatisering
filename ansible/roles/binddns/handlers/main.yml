# SPDX-License-Identifier: MIT-0
---
# handlers file for roles/binddns

- name: Restart_named
  ansible.builtin.systemd:
    name: named
    state: reloaded

- name: Reload_firewalld
  ansible.builtin.systemd:
    name: firewalld
    state: reloaded
